ext {
    versionConfig = [
            compileSdkVersion    : 29,
            buildToolsVersion    : "30.0.2",
            minSdkVersion        : 19,
            targetSdkVersion     : 29,
            versionCode          : 1,
            versionName          : "1.0"
    ]

    dependConfig = [
            glide                : "4.11.0",
    ]
}